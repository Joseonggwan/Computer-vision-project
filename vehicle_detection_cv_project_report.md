# 컴퓨터 비전 프로젝트 사전 기획 보고서

## 1. 프로젝트 개요

### 1.1 기본정보

| 항목 | 내용 |
|---|---|
| **프로젝트명** | CNN 기반 차량/비차량 이미지 분류 및 오분류 분석 |
| **연도** | 2026 |
| **Github 저장소** | `https://github.com/Joseonggwan/Computer-vision-project` |
| **데이터셋** | Kaggle - Vehicle Detection Image Set |
| **데이터 출처** | Kaggle, Baris Dincer |
| **프로젝트 선정 이유** | 차량과 비차량을 구분하는 이진 이미지 분류 문제는 컴퓨터 비전의 대표적인 응용 사례이며, 비교적 균형 잡힌 대규모 이미지 데이터셋을 활용하여 CNN의 이미지 특징 추출 및 분류 과정을 실습하기에 적합하다. 또한 기본 CNN과 전이학습 모델을 비교하고 오분류 사례를 분석하여 모델의 성능과 한계를 확인할 수 있다는 점에서 본 프로젝트의 주제로 선정하였다. |

### 1.2 문제정의

#### Task의 종류
**Binary Image Classification (이진 이미지 분류)**

입력 이미지가 `Vehicle`인지 `Non-Vehicle`인지 두 개의 클래스 중 하나로 분류한다. 본 데이터셋은 차량의 정확한 위치를 찾는 Object Detection이 아니라, 이미지 전체를 대상으로 차량 존재 여부를 판단하는 이미지 분류 문제로 정의한다.

#### 문제 정의
차량이 포함된 이미지와 차량이 포함되지 않은 이미지를 학습 데이터로 사용하여 이미지의 시각적 특징을 자동으로 추출하고, 새로운 입력 이미지가 차량인지 비차량인지 분류하는 딥러닝 모델을 구축한다.

#### 목표
1. 차량/비차량을 안정적으로 구분할 수 있는 이미지 분류 모델 구축
2. CNN의 이미지 특징 추출 및 분류 성능 확인
3. 전이학습 모델 적용을 통해 성능 개선 가능성 검토
4. Accuracy뿐 아니라 Precision, Recall, F1-score 및 Confusion Matrix를 활용하여 모델 성능 평가
5. 오분류 이미지를 분석하여 모델의 한계와 개선 방향 도출

---

## 2. 모델 설계

### 2.1 모델 아키텍처

#### 사용 모델명
**Baseline CNN + Transfer Learning 기반 모델 비교**

본 프로젝트에서는 직접 구성한 CNN을 Baseline으로 사용하고, 이후 **ResNet18 / MobileNetV3 / EfficientNet-B0** 등의 사전학습 모델을 적용하여 성능을 비교한다. 최종 모델은 실제 실험 결과를 기준으로 선정하며, 사전에 특정 모델의 성능 우위를 가정하지 않는다.

#### 핵심 알고리즘 및 메커니즘

**CNN (Convolutional Neural Network)**  
합성곱층을 이용하여 이미지의 에지, 형태, 질감 등의 공간적 특징을 단계적으로 추출한 뒤, 추출된 특징을 이용하여 차량/비차량을 분류한다.

**Transfer Learning**  
대규모 이미지 데이터셋에서 사전학습된 모델의 특징 추출 능력을 활용하고, 본 데이터셋에 맞게 마지막 분류 계층 등을 fine-tuning하여 비교적 적은 학습 비용으로 분류 성능을 확보한다.

---

### 2.2 요구사항

아래 사양은 본 프로젝트의 이미지 분류 모델 학습을 위한 **권장 기준**이며, 실제 요구사항은 입력 해상도, batch size, 모델 크기 및 학습 방식에 따라 달라질 수 있다.

| 구분 | 최소 사양 | 권장 사양 |
|---|---|---|
| **CPU** | 4코어급 Intel Core i5 / AMD Ryzen 5 이상 | 6~8코어급 Intel Core i7 / AMD Ryzen 7 이상 |
| **RAM** | 16 GB | 32 GB |
| **GPU** | CUDA 지원 GPU, VRAM 4 GB 이상 | CUDA 지원 GPU, VRAM 8 GB 이상 |
| **필요 VRAM** | 약 4 GB 이상 | 약 8 GB 이상 |
| **저장공간** | 데이터셋 및 환경 포함 약 10 GB 이상 여유 공간 | SSD 기준 20 GB 이상 여유 공간 |
| **OS** | Windows 10/11, macOS 또는 Linux | Windows 11 / Linux 권장 |
| **Python** | Python 3.10 이상 | Python 3.10~3.12 |
| **주요 라이브러리** | PyTorch 또는 TensorFlow, NumPy, OpenCV, scikit-learn, Matplotlib | 동일 |
| **개발환경** | VS Code / Jupyter Notebook / Google Colab | VS Code + Jupyter 또는 Google Colab |

> **참고:** macOS에서 Apple Silicon GPU(MPS)를 사용하는 경우 CUDA 전용 요구사항과는 다르게 동작하므로, NVIDIA CUDA GPU가 반드시 필요한 것은 아니다. 또한 본 프로젝트는 Google Colab의 GPU 환경에서도 수행할 수 있다.

---

## 3. 데이터셋 분석

### 3.1 데이터 특징

| 항목 | 내용 |
|---|---|
| **데이터 유형** | RGB 이미지 데이터 |
| **문제 유형** | 이진 이미지 분류 |
| **해상도** | 원본 데이터의 이미지 해상도는 이미지별 차이가 있을 수 있으므로, 학습 단계에서 **64×64×3 RGB**로 통일하는 방식을 기준으로 설계 |
| **클래스 수** | 2개 |
| **클래스** | `Vehicles`, `Non-Vehicles` |
| **전체 데이터 수** | 17,760장 |
| **Vehicles** | 8,792장 |
| **Non-Vehicles** | 8,968장 |
| **총 용량** | 실제 다운로드 파일 기준으로 측정 필요 |
| **데이터 분할 비율** | Train 80% / Test 20%를 기본으로 사용하며, 학습 과정에서는 Train의 일부를 Validation으로 분리 |
| **예상 Train/Test 수** | Train 14,208장 / Test 3,552장 |

본 데이터셋은 Kaggle 설명상 `Vehicles`와 `Non-Vehicles`의 2개 라벨, 총 17,760개의 이미지로 구성되어 있다. 별도 연구에서 확인된 클래스별 수는 `Non-Vehicles` 8,968장, `Vehicles` 8,792장이다. 따라서 클래스 간 데이터 수가 크게 차이나지 않는 비교적 균형 잡힌 데이터셋으로 볼 수 있다. citeturn0search0turn0search2

64×64 RGB 입력 크기는 동일 데이터셋을 활용한 공개 구현 사례에서 사용된 전처리 기준이다. 따라서 프로젝트에서는 원본 이미지의 실제 해상도를 먼저 확인한 뒤, 학습 입력 크기를 64×64로 통일하고 필요하면 128×128 또는 224×224와 비교한다. citeturn0search1

**총 용량은 Kaggle에서 실제 다운로드한 파일의 압축 크기와 압축 해제 후 크기가 다를 수 있으므로, 프로젝트 환경에서 파일 크기를 직접 측정하여 최종 보고서에 기입한다.**

---

## 4. 적용 가능성 검토

### 4.1 적용 가능성 검토

#### 참고 프로젝트의 한계

기존에 동일 데이터셋을 사용한 연구 및 구현 사례에서는 CNN 또는 InceptionV3, Xception, MobileNet 계열, VGG 계열, EfficientNet 계열 등 다양한 모델을 이용하여 차량/비차량 분류를 수행하였다. citeturn0search2

그러나 기존 사례에는 다음과 같은 한계가 있을 수 있다.

1. 단순히 Accuracy 중심으로 성능을 비교할 경우 특정 클래스에서 발생하는 오분류 특성을 충분히 파악하기 어렵다.
2. 동일 데이터셋에 대해 높은 정확도가 보고된 사례가 있어도, 데이터 전처리 및 분할 방식에 따라 결과가 달라질 수 있다.
3. 64×64와 같이 낮은 해상도의 입력을 사용하면 작은 차량이나 복잡한 배경의 세부 특징이 손실될 가능성이 있다.
4. 학습 데이터와 테스트 데이터가 유사한 특성을 가질 경우 실제 환경에서의 일반화 성능을 충분히 반영하지 못할 수 있다.
5. 차량/비차량 분류만 수행하기 때문에 이미지 속 차량의 위치, 종류, 크기 등을 탐지하거나 구분할 수 없다.

#### 내 프로젝트 적용점 및 개선 방안

본 프로젝트에서는 기존 사례를 그대로 재현하는 것보다 다음과 같은 방향으로 개선한다.

- **Baseline CNN 구축:** 단순한 CNN을 먼저 구현하여 기준 성능을 확보한다.
- **Transfer Learning 비교:** ResNet18, MobileNetV3, EfficientNet-B0 등을 적용하여 모델 구조에 따른 성능 차이를 비교한다.
- **Data Augmentation:** 회전, 좌우 반전, 밝기 변화, crop 등의 augmentation을 적용하여 다양한 환경에 대한 일반화 가능성을 높인다.
- **다중 평가 지표:** Accuracy뿐 아니라 Precision, Recall, F1-score, Confusion Matrix를 함께 사용한다.
- **오분류 분석:** False Positive와 False Negative 이미지를 직접 확인하여 모델이 어떤 상황에서 잘못 판단하는지 분석한다.
- **입력 해상도 비교:** 64×64와 더 높은 해상도의 입력을 비교하여 해상도가 성능에 미치는 영향을 확인한다.
- **과적합 분석:** Training/Validation Loss와 Accuracy를 관찰하고 Early Stopping 및 Dropout 등을 활용한다.

#### 예상되는 문제

1. **배경 의존 문제**  
   모델이 차량의 형태 자체보다 도로, 주차장, 하늘 등의 배경 특징을 학습할 가능성이 있다.

2. **차량 크기 및 위치 문제**  
   차량이 작거나 이미지의 일부에만 존재하는 경우 차량의 특징을 충분히 추출하지 못할 수 있다.

3. **낮은 해상도에 따른 정보 손실**  
   64×64로 이미지를 축소할 경우 번호판, 헤드라이트 등 세부적인 시각 정보가 손실될 수 있다.

4. **실제 환경과 데이터셋의 차이**  
   학습 데이터와 실제 도로 환경의 조명, 날씨, 촬영 각도, 차량 종류 등이 다를 경우 성능이 낮아질 수 있다.

5. **데이터 누수 가능성**  
   동일하거나 매우 유사한 이미지가 Train과 Test에 동시에 포함되면 테스트 성능이 실제보다 높게 측정될 수 있으므로 데이터 분할 전 중복 이미지 확인이 필요하다.

6. **클래스 경계의 모호성**  
   차량이 부분적으로 가려져 있거나 차량과 유사한 형태의 물체가 포함된 이미지에서는 사람이 판단하기에도 애매한 사례가 발생할 수 있다.

#### 종합 검토

본 데이터셋은 17,760개의 이미지와 2개의 클래스로 구성되어 있어 이진 이미지 분류 모델을 실험하기에 적합하다. 특히 클래스별 데이터 수가 비교적 균형적이므로 기본적인 CNN 모델부터 전이학습 모델까지 단계적으로 비교할 수 있다. citeturn0search0turn0search2

다만 본 프로젝트의 목표는 높은 Accuracy 하나만을 얻는 것이 아니라, **여러 모델의 성능을 동일한 조건에서 비교하고 오분류 사례를 분석하여 실제 환경에서의 적용 가능성과 한계를 확인하는 것**으로 설정한다. 이후 필요하다면 차량의 위치까지 탐지할 수 있는 Object Detection 문제로 확장할 수 있다.
